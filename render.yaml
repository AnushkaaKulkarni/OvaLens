services:
  - type: web
    name: ovalens-backend
    env: python
    plan: free
    root: backend
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
